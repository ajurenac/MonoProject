@model Project.MVC.ViewModels.VehicleModelViewModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>VehicleModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>            
            <div class="form-group">
                <label asp-for="DropDownMakeId" class="control-label">Vehicle Make</label>
                <select asp-for="DropDownMakeId" class="form-control" asp-items="@(new SelectList(Model.Makes, "Id", "Name"))">
                    <option value="">-- Select Make --</option>
                </select>
            </div>
            <div class="form-group">
                <label asp-for="VehicleModel.Name" class="control-label">Model Name</label>
                <input asp-for="VehicleModel.Name" class="form-control" />
                <span asp-validation-for="VehicleModel.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="VehicleModel.Abrv" class="control-label">Model Abbreviation</label>
                <input asp-for="VehicleModel.Abrv" class="form-control" />
                <span asp-validation-for="VehicleModel.Abrv" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>
